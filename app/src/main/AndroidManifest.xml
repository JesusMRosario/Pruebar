<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.inacap.gogogym">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="cl.inacap.gogogym.MainActivity5"></activity>
        <activity android:name="cl.inacap.gogogym.MainActivity4" />
        <activity android:name="cl.inacap.gogogym.MainActivity3" />
        <activity android:name="cl.inacap.gogogym.MainActivity2" />
        <activity android:name="cl.inacap.gogogym.MainActivity" />
    </application>

</manifest>